services:
    vector-db:
        image: qdrant/qdrant
        ports:
            - 6333:6333


# to run compose
# >> docker compose up


# After that you need to install Langchain
#  >> pip install -qU langchain-community pypdf