# Template for .env — copy, then paste your real keys.
#
#     cp .env.example .env && chmod 600 .env
#
# This file IS committed and must never hold a real key.
# The .env you create from it is gitignored by the repo-root .gitignore.
#
# Rules that bite if you break them:
#   - No space after '='.  KEY= sk-...  fails under `set -a; source .env`.
#   - No quotes needed.    OPENAI_API_KEY=sk-proj-abc  (not "sk-proj-abc")
#   - One KEY=VALUE per line, no trailing spaces.

# ── Provider & model ─────────────────────────────────────────────────────────
# Which provider every chain uses: openai | groq. Not secrets.
# openai is what the architecture diagram calls for, but it is paid - an empty
# balance fails with 429 credit_balance_exhausted. groq is the free fallback.
LLM_PROVIDER=groq
OPENAI_MODEL=gpt-4.1-mini
GROQ_MODEL=openai/gpt-oss-120b

# ── API keys ─────────────────────────────────────────────────────────────────
# Only the provider you actually use needs a real value; leave the rest empty.

# https://platform.openai.com/api-keys   (paid — needs a funded account)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# https://console.groq.com/keys          (free tier — the usual fallback)
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# https://aistudio.google.com/apikey     (free daily quota)
# Note: this project uses GOOGLE_API_KEY, while 01_prompt_serialization calls
# the very same key GEMINI_API_KEY. Set both if you use both projects.
GOOGLE_API_KEY=AIzaxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# https://console.anthropic.com/settings/keys   (paid)
ANTHROPIC_API_KEY=
